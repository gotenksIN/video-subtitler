[project]
name = "video-subtitler"
version = "0.1.0"
description = "Gemini-powered CLI for generating VTT subtitles from video."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.0.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "webvtt-py>=0.5.1",
]
