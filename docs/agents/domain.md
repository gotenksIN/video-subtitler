# Domain docs

Use the single-context domain layout.

Before exploring, read `CONTEXT.md` and relevant ADRs under `docs/adr/`.
If these paths do not exist, proceed without asking to create them.
The domain-modeling workflows create them when needed.

Use terms defined in `CONTEXT.md` rather than introducing synonyms.
Surface any conflict with an existing ADR explicitly.
